on:
  push:
    branches-ignore:
      - master
    paths:
      - '**/*.sh'
      - 'image/Dockerfile'

  pull_request:
    paths:
      - '**/*.sh'
      - 'image/Dockerfile'

name: "Test"
permissions: {}

jobs:
  shellcheck:
    name: Test
    uses: ./.github/workflows/check.yml
